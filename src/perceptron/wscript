#!/usr/bin/python

def build(bld):
    bld.shlib(source = ['perceptron.cc'],
              target = 'perceptron',
              includes=bld.SRCPATH)

    bld(features = 'cxx cprogram',
        source = ['perc_test.cc'],
        target = 'perc_test',
        includes = bld.SRCPATH,
        use = ['perceptron'])
